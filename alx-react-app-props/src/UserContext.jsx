import { createContext } from "react";

// Initialize a new context called UserContext
const UserContext = createContext();

// Export the UserContext so it can be used in other parts of the application
export default UserContext;
